# Banco de dados (SQLite local para desenvolvimento).
# Em produção (Vercel/Railway/Render), troque por Postgres e ajuste o provider
# do Prisma em prisma/schema.prisma.
DATABASE_URL="file:./db/personaforge.db"
